name: Lead HQ sprites from reference sheet
on:
  workflow_dispatch:
jobs:
  art:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
    steps:
      - uses: actions/checkout@v4
      - name: Decode sprites
        run: |
          python3 << 'INNER'
          import base64, pathlib, tarfile, io
          # placeholder - replaced below
          print('use artifact approach')
          INNER
