name: Party2 Father
on:
  workflow_dispatch:
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python3 tools/patch_party2_tab.py
      - name: Commit
        run: |
          git config user.name "Grok C"
          git config user.email "76492590+xenodorian@users.noreply.github.com"
          git add src/game/engine.ts
          git diff --staged --quiet && echo no changes && exit 0
          git commit -m "Leg 2.7: Tab/Q swaps Max/Father party on overworld"
          git push
