name: Gauntlet 2.4
on:
  workflow_dispatch:
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python3 tools/patch_gauntlet_24c.py
      - name: Commit
        run: |
          git config user.name "Grok C"
          git config user.email "76492590+xenodorian@users.noreply.github.com"
          git add src/game/engine.ts
          git diff --staged --quiet && echo no changes && exit 0
          git commit -m "Leg 2.4: Heavenfall grave win skips mercy, Slayer title, credits"
          git push
