name: restore-engine
on:
  workflow_dispatch:
jobs:
  restore:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Restore opal frames, sync parts, mark Opal done
        run: |
          python3 << 'PY'
          import base64, json, subprocess
          from pathlib import Path
          from PIL import Image

          sprites = {
            "opal-1.png": open("/dev/null").read() if False else None,
          }
          PY
