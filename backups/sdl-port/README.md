# SDL2 / R36S PortMaster snapshot (removed)

Taken from xenodorian/CryMon `native/` before it was deleted.
C sources and PortMaster scripts only — binaries (`crymon.aarch64`,
`gfx_blob.bin`) and zips were generated artifacts and are not stored here.

Do not restore unless the user asks. Remaining ports: web and Dreamcast.
